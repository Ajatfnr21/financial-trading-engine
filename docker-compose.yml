version: '3.8'

services:
  trading:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
    command: python -m uvicorn engine.main:app --host 0.0.0.0 --port 8000 --reload
